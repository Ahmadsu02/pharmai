[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd israel-drugs-mcp-server && npm install --omit=dev"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
