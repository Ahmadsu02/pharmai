[phases.setup]
nixPkgs = ["nodejs_20", "python313"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "bash -c 'cd israel-drugs-mcp-server && npm install && npm run build'"
]

[phases.build]
cmds = []

[start]
cmd = "bash start.sh"
